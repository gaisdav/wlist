// Zod schemas describing request/response payloads of Edge Functions.
// Imported in two places: the function itself (input parsing) and the client
// (call typing + response validation). See docs/architecture.md §9.
export {};
