---
description: Конвенции packages/core (бизнес-логика, hooks, маршруты, токены)
globs: packages/core/**/*.{ts,tsx}
alwaysApply: false
---

# `@wlist/core` — конвенции

Подробности — в [`docs/architecture.md`](mdc:docs/architecture.md) §3 и §7.

## Структура (layer-based, не feature-based)

```
src/
├── entities/   # Zod поверх `api/generated/database.zod` — по умолчанию snake_case как в БД
├── services/   # Бизнес-операции, чистые функции, принимают ApiClient, плоско
├── hooks/      # React-обёртки поверх services + TanStack Query, ПО ДОМЕНАМ
├── routes/     # Декларативные маршруты (см. ниже)
├── tokens/     # Дизайн-токены (plain TS)
├── i18n/       # Словари переводов + типы ключей (без init i18next)
├── lib/        # Платформо-агностичные утилиты
└── config/     # Constants, queryKeys factory
```

**Правила вложенности:**

- `entities/`, `services/` — плоско (1 файл на сущность). Если файл превышает ~250 строк — выносим в подпапку.
- `hooks/` — **всегда по доменам**, даже если в домене один хук:

```
hooks/
├── auth/
│   ├── useCurrentUser.ts
│   └── index.ts
├── wishes/
│   ├── useMyWishes.ts
│   ├── useWish.ts
│   ├── useCreateWish.ts
│   └── index.ts
└── slots/
    ├── useSlots.ts
    ├── useBookSlot.ts
    └── index.ts
```

Импорт всегда через barrel: `import { useMyWishes } from '@wlist/core/hooks/wishes'`.

## Платформо-агностичность

`core` **не должен** импортировать:
- `react-dom`, `window`, `document`, любые DOM API
- `@telegram-apps/*`
- что-либо из `apps/*`
- `SupabaseApiClient` (только интерфейс `ApiClient` из `@wlist/api`)

React импортируется **только** в `hooks/`. `services/` и `entities/` — чистый TS.

## Entities

- Берём `public*RowSchema` из `@wlist/api/generated/database.zod`, затем `.extend()` / `.refine()` под инварианты, которых нет в CHECK (URL ссылки, список валют и т.п.).
- **По умолчанию не делаем `.transform()` в camelCase** — тип строки совпадает с `Database['public']['Tables'][…]['Row']`, меньше дублирования и расхождений после `db:codegen`.
- Не описывай табличную строку «с нуля» (`z.object({ … })`) — сначала generated row-схема, потом узкие правки.

```ts
import { publicWishesRowSchema } from '@wlist/api/generated/database.zod';

export const wishSchema = publicWishesRowSchema.extend({
  link: z.url().nullable(),
  currency: z.string().refine(isWishCurrencyCode),
});

export type Wish = z.infer<typeof wishSchema>;
```

## Services

```ts
// ❌ BAD: импортит supabase напрямую
import { supabase } from '@wlist/api';
export async function listWishes(ownerId: string) { ... }

// ✅ GOOD: принимает ApiClient
import type { ApiClient } from '@wlist/api';
export const wishesService = {
  async listByOwner(api: ApiClient, ownerId: string): Promise<Wish[]> {
    const wishes = await api.wishes.listByOwner(ownerId);
    return wishes.map((row) => wishSchema.parse(row));
  },
};
```

## Hooks

- Получают `ApiClient` через `useApiClient()` (Context).
- Используют ключи из `queryKeys` factory — никаких string literals.

```ts
export function useUserWishes(ownerId: string) {
  const api = useApiClient();
  return useQuery({
    queryKey: queryKeys.wishes.byOwner(ownerId),
    queryFn: () => wishesService.listByOwner(api, ownerId),
  });
}
```

## Даты и время

- Все форматирование дат/относительного времени — через `@wlist/core/lib` (`dateUtils.ts`): `formatDate`, `formatRelativeTime`, `isValidDateString` и т.д.
- **Не** вызывай `new Date(iso).toLocaleString()` / `toLocaleDateString()` в `apps/tma` или `services/` напрямую.
- В UI передавай `locale: i18n.language` из `react-i18next` (fallback в утилите — `DEFAULT_DATE_LOCALE`, сейчас `en`).
- Для ленты и списков предпочитай `formatRelativeTime` («2 hr. ago» / fallback на `dayMonthYear`); для статичных дат — `formatDate` с явным `DateFormat`.
- Юнит-тесты на ветви — рядом: `lib/dateUtils.test.ts`.

## QueryKeys

Все ключи — в `core/config/queryKeys.ts`. Никогда не пиши `['wishes', id]` в компоненте напрямую.

## Routes

Новый экран → новый маршрут в `routes/routes.ts` со схемой параметров. Билд URL только через `routes.X.build({...})`.
