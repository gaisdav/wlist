# Этап 03. MVP — Слоты совместной покупки

## Цель

Реализовать киллер-фичу сервиса: возможность скинуться на дорогой подарок или собрать средства на желание, координируя процесс прозрачно для всех, кроме самого получателя.

## Зависимости

- Этап 02 (Wishlist CRUD).

## Скоуп

### 1. Расширение модели `wishes`

Новые поля:

- `is_collaborative` (`boolean`, default false) — **group gift**: несколько участников делят подарок через **слоты** (`N/cap` в UI). Если `false` — обычное желание wishlist: одна **резервация** на весь подарок (без термина «слоты» в UI).
- `max_slots` (`int`, nullable) — **верхняя граница** числа занятых слотов (сколько слотов **максимум** может быть занято на это желание). По факту активных строк может быть **меньше**. Если **`null`** — автор не задаёт свой потолок; тогда эффективный потолок **`50`** активных слотов: в UI не предлагаем бронь сверх 50, **и на сервере** (триггер / проверка при INSERT) тот же потолок, чтобы нельзя было обойти клиент. Если **`max_slots` задан** — потолок ровно это число (например `5` → не больше пяти активных строк).
- `copy_lines` (`jsonb`, **nullable**) — **JSON-массив строк** `["…", "…"]` (не `text[]`): до **5** элементов, каждая строка **не длиннее 256 символов** (`CHECK` в миграции + зеркальная валидация в `@wlist/core`). Автор вводит поля при создании/редактировании (реквизиты, адрес одной строкой и т.п.); в UI у каждой непустой строки — **иконка «копировать»** рядом с текстом.

**Не делаем:** отдельного типа/enum «вид желания» (`wish_kind`), отдельного поля `payment_details` с фиксированной схемой — всё произвольное вводится в этих строках.

### 2. Модель бронирования

Таблица `public.wish_slots`:

- `id` (`uuid`, PK)
- `wish_id` (`uuid`, FK → `wishes.id`, on delete cascade)
- `booked_by` (`uuid`, FK → `profiles.id`)
- `status` (`enum`: `active` | `cancelled`) — отмена не удаляет запись (нужна история)
- `created_at`, `cancelled_at`

**Group gift (`is_collaborative = true`):** один пользователь может занять **несколько** активных слотов (несколько строк с одним `booked_by`). Потолок: **`slot_cap = COALESCE(wishes.max_slots, 50)`**.

**Обычное желание (`is_collaborative = false`):** та же таблица `wish_slots`, но **`slot_cap = 1`** — классическая резервация «кто-то взял подарок» (в UI слово «slot» не используем).

Ограничение целостности: число активных строк **не превышает** `wish_slots_effective_cap(wish_id)` (триггер + RLS). Константу **`50`** для group gift — в `@wlist/core` (`WISH_SLOTS_DEFAULT_CAP`).

### 3. Бизнес-логика

Потолок: **`wish_slots_effective_cap(wish_id)`** — `1` для обычного желания, `COALESCE(max_slots, 50)` для group gift.

- **Расчёт статуса** (computed / view, по желанию):
  - `open` — `active_count < cap`;
  - `collected` — `active_count >= cap` (для обычного wish = «зарезервировано»).
- **Бронирование / резервация** (любое чужое неархивное желание):
  - нельзя забронировать/зарезервировать **своё** желание;
  - нельзя на **архивное**;
  - при `active_count >= cap` — блок INSERT (RLS + trigger);
  - **Обычное желание:** одна кнопка «Reserve» / «Cancel my reservation»; RPC `book_wish_slots` только с `count = 1`.
  - **Group gift:** UI слотов — «взять 1 / все оставшиеся» в пределах `cap`; терминология «слоты» только здесь.
- **Отмена:** `status = cancelled` (история сохраняется); уведомления — этап 10.
- **Видимость для не-автора:** статус «Available» / «Reserved» (обычное) или `active/cap` (group gift). **Забронировавший** видит **«Your reservation»** (имена других по-прежнему не показываем в MVP).

### 4. RLS

- `wish_slots`:
  - **SELECT:** все авторизованные **кроме автора** желания (это «секретная» часть). Автор не должен видеть, кто забронировал.
  - **INSERT/UPDATE:** только сам участник (`booked_by = auth.uid()`).
- Реализация скрытия от автора: RLS-политика с условием `auth.uid() != (select owner_id from wishes where id = wish_id)`.

### 5. Уведомления

В MVP — без Telegram-бота, без push.

- В UI Mini App при открытии экрана желания — fresh-fetch и баннер «слот освободился» (если у пользователя был активный слот, и появилось событие отмены чужого слота).
- Полноценные уведомления — этап 10.

### 6. UI

1. **Карточка желания** (только **не автор**, не архив):
   - бейдж «Group gift», если `is_collaborative`;
   - индикатор резервации: **Available** / **Reserved** (обычное) или **`active/cap` booked** (group gift);
   - если текущий пользователь зарезервировал — **Your reservation** (приоритет над общим статусом);
   - `copy_lines` + копирование на детали (как раньше).
2. **Деталка** — компактный индикатор в шапке (те же правила) + блок действий:
   - **Обычное желание:** Reserve / Cancel my reservation (без слов «slot»);
   - **Group gift:** секция «Gift slots», счётчик, несколько слотов, cancel по слотам.
3. **Список участников** (group gift) — виден не-автору wish (не owner list).
4. **«Мои бронирования»** — `/me/bookings` (и резервации, и слоты).
5. **Форма wish** — флаг group gift, `max_slots`, `copy_lines`.

### 7. Клиентский слой

- `packages/api`: `slots.list(wishId)`, `slots.book(wishId, …)` (сигнатура под число слотов или повторное бронирование — по выбранному UX), `slots.cancel(slotId)`, `myBookings.list()`.
- `packages/core`: `useSlots(wishId)`, `useBookSlot()`, `useCancelSlot()`, `useMyBookings()`.

## Вне скоупа

- Telegram-уведомления об освободившихся слотах (этап 10).
- Секретный групповой чат для забронировавших (этап 10).
- Real-time обновление статусов (этап 11).
- Реальная оплата через приложение — её и не будет, мы только координатор.

## Ключевые решения

- **«Скрыто от автора» реализуем строго на RLS**, а не на уровне UI — иначе можно подсмотреть через DevTools/прямой API-запрос.
- **Отмена не удаляет** — нужна история и для аудита, и для будущих фич (рейтинг активных дарителей).
- **Не реализуем платежи внутри приложения** — это базовая ценность концепта (простота + отсутствие финансовой ответственности).
- **`max_slots`** — только для **group gift**; при `null` cap **`50`**. Обычное желание: cap **`1`** на уровне БД, в UI — «reserve», не «slots».
- **Одна таблица `wish_slots`** для стандартной резервации и для group gift — без второй сущности.
- **`copy_lines`**: **`jsonb`** = JSON-array строк, **≤ 5** элементов, каждая строка **≤ 256** символов; без отдельной схемы `payment_details`.

## Чек-лист задач

- [x] Миграция: расширение `wishes` (`is_collaborative`, `max_slots`, `copy_lines` + `CHECK` на json-массив, длину массива и **256** символов на строку) + создание `wish_slots` **без** колонки `note`
- [x] RLS-политики (особенно — скрытие слотов от автора)
- [x] Триггер/функция: блокировка превышения **`COALESCE(wishes.max_slots, 50)`** по числу активных строк `wish_slots`
- [x] SQL view / функция для computed-статуса желания (учёт **`slot_cap`** и `active_count`)
- [x] Константа в `@wlist/core` для **`50`** (и при необходимости **256**) + валидация `copy_lines` в доменной схеме
- [x] API-слой
- [x] Hooks
- [x] UI карточки желания с бейджем и счётчиком слотов
- [x] UI бронирования (в т.ч. несколько слотов одним пользователем, «все оставшиеся» — по согласованному UX)
- [x] UI «Мои бронирования»
- [x] Расширенная форма желания: совместная покупка, `max_slots`, до 5 полей `copy_lines` + копирование на карточке/детале
- [x] Ручная проверка RLS (особенно — что автор не видит слоты ни через UI, ни через прямой API). Автоматизированный test suite — после первой беты, см. этап 04.

## Definition of Done

- Можно создать желание с совместной покупкой и опциональным потолком `max_slots` (или без него — тогда действует лимит **50** слотов).
- Друзья могут занимать и отменять слоты; **один пользователь может держать несколько активных слотов** на одно желание.
- Автор желания не видит ни список, ни количество забронированных слотов **через любой способ** (UI, прямой API, REST).
- При достижении **`slot_cap`** (`COALESCE(max_slots, 50)`) желание получает статус «Собрано» в смысле слотов.
- Строки `copy_lines` отображаются с копированием в буфер обмена.

## Открытые вопросы

- Что если желание архивировано/удалено, а резервация была активна — какое сообщение участнику?

**Закрыто (зафиксировано):** `copy_lines` — **`jsonb`** JSON-array; **256** символов на строку; group gift cap **50** на сервере; **автор не видит** резервации; **резервировавший** видит **Your reservation**; обычные wishes — reserve cap **1**, без UI «slots».

## Backlog / TODO

- **Лента (feed): N+1 запросов** `slots.listByWish` на каждую `WishCard` (как с лайками). Позже: RPC/view или денормализованный агрегат **без** раскрытия имён автору wish.
